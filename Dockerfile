#  От какого образа из dockerhub наследуемся
FROM node:16-alpine

# Основная(рабочая дирректория в контейнере)
WORKDIR /app

# Копируем в контейнер package.json и package-lock.json в основную дирректорию
COPY package*.json ./

# Установка зависимостей
RUN npm install

# Копируем весь код проекта в основную дирректорию
COPY . .

# Определяем какой порт ИСПОЛЬЗОВАТЬ
EXPOSE 3000

# Какой командой запускаем проект
CMD [ "npm", "start" ]
